input.osm.pbf.file = "input_file_path"
input.asset.file.directory = "asset_directory"
input.asset.file.hierarchic = false
output.csv.directory = "output_file_path"

generation.lv {
  voltageLevel {
    id = "lv"
    vNom = [0.4, 1.0]
  }
  distinctHouseConnections = true
  boundaryAdminLevel {
    starting = 2
    lowest = 8
  }
}

generation.mv {
  voltageLevel {
    id = "mv"
    vNom = [10.0, 20.0, 30.0]
  }
}
